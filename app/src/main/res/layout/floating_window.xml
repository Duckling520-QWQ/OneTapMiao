<?xml version="1.0" encoding="utf-8"?>
<!--
  悬浮窗：状态圆点 + 图标 + 喵按钮 横向胶囊。

  布局说明：
    [ ● 状态点 10dp ][ 🐱 图标 56dp ][ 喵 40dp ]
    ● 状态点：绿 = 文本改写中，灰 = 已暂停（与用户设置的透明度解耦，任何透明度下都看得清）
    点图标（含状态点一侧） = 切换总开关
    点喵   = 一键加喵（走输入法通道，微信可用）
    整体拖动 = 移动悬浮窗

  注意：根布局必须是 horizontal，FloatingWindowService.handleTap()
        用 event.getX() 和 iconView.getRight() 比较来判断点在哪半边。
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:padding="4dp"
    android:background="@android:color/transparent">

    <!-- v1.1.1：改写状态圆点（绿=开 / 灰=暂停），颜色由服务更新 -->
    <View
        android:id="@+id/status_dot"
        android:layout_width="10dp"
        android:layout_height="10dp"
        android:layout_marginEnd="3dp"
        android:background="@drawable/circle_background"
        android:contentDescription="改写状态" />

    <ImageView
        android:id="@+id/floating_icon"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:background="@drawable/circle_background"
        android:src="@mipmap/ic_launcher"
        android:scaleType="centerCrop"
        android:contentDescription="悬浮窗图标" />

    <TextView
        android:id="@+id/btn_miao"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_marginStart="2dp"
        android:background="@drawable/miao_button_bg"
        android:text="喵"
        android:textColor="#FFFFFF"
        android:textSize="18sp"
        android:textStyle="bold"
        android:gravity="center"
        android:includeFontPadding="false"
        android:contentDescription="一键加喵" />

</LinearLayout>
